# Pinned to the versions verified against the shipped model artifacts.
#
# Install into the project virtualenv, NOT a base/conda environment: shap
# requires numpy>=2, which conflicts with numpy<2 packages such as autogluon,
# gensim, catboost and contourpy.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r Backend/requirements.txt
#
# The backend auto-detects .venv; override with ML_PYTHON=/path/to/python.
# Note: OMP_NUM_THREADS=1 is required at runtime (set by ml_service/Dockerfile
# and documented in README.md's Quick Start) because LightGBM's bundled
# OpenMP runtime segfaults on macOS when more than one thread is used
# alongside certain other native libraries in-process.
numpy>=2.0.0
pandas>=2.0.0
scikit-learn>=1.0.0
lightgbm>=4.0.0
shap>=0.40.0

# Test harness only (Backend/tests/test_ml_predictor.py).
pytest==8.3.4
