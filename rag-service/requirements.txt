# RAG chatbot service: FastAPI + LangChain, local HuggingFace embeddings,
# a local Chroma vector store built from knowledge/*.md, and Groq for
# generation. Kept in its own venv/requirements -- separate from
# Backend/requirements.txt's pinned ML stack, since sentence-transformers
# pulls in its own (large) torch dependency unrelated to the LightGBM
# scoring path.
fastapi
uvicorn
python-dotenv
langchain
langchain-core
langchain-community
langchain-text-splitters
langchain-huggingface
langchain-chroma
langchain-groq
sentence-transformers
chromadb
PyMuPDF
